let a;
let b;
let x;

const divide = () => {
   a = 2000
   b = 100
   return Math.abs(a / b);
}

const square = () => {
  x = 2
  return Math.abs(x**x)
}


const add = () => {
   a = 3
   b = 4
   return Math.abs(a + b)
}